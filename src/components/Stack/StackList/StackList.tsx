import { TStackItem } from '@/types';

import React from 'react';

import styles from './StackList.scss';

type TProps = {
  stack: TStackItem[];
};

export const StackList = (props: TProps) => {

};

