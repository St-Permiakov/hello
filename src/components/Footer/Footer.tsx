import React from 'react';

import styles from './Footer.scss';

export const Footer = () => {
    return (
        <div className={styles.root}>
            Footer Component
        </div>
    );
};

